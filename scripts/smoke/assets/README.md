# Smoke Assets

Drop your test image here as `order.jpg` (or set a custom path via SMOKE_IMAGE_PATH in .env.smoke).

Recommended: a screenshot of an order list or a receipt. Supported formats: jpg/jpeg/png/webp.

This folder is ignored by git (no binary committed by default).
